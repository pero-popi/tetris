/**
 * 矩形領域を作成します。
 */
export default interface Rect {
  /**
   * x座標の位置(px)です。
   */
  x: number;

  /**
   * y座標の位置(px)です。
   */
  y: number;

  /**
   * 横幅(px)です。
   */
  width: number;

  /**
   * 縦幅(px)です。
   */
  height: number;
}
